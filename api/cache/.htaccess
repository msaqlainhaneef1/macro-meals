# Legacy folder — file cache removed. Block direct access if any old JSON remains.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Deny from all
</IfModule>
