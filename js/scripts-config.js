const EXTERNAL_SCRIPTS=[];const EXTERNAL_BODY_SCRIPTS=[];(function injectExternalScripts(){EXTERNAL_SCRIPTS.forEach(function(snippet){if(!snippet||snippet.trim().startsWith('//'))return;var temp=document.createElement('div');temp.innerHTML=snippet;Array.from(temp.children).forEach(function(el){document.head.appendChild(el.cloneNode(true));});});EXTERNAL_BODY_SCRIPTS.forEach(function(snippet){if(!snippet||snippet.trim().startsWith('//'))return;var temp=document.createElement('div');temp.innerHTML=snippet;Array.from(temp.children).forEach(function(el){document.body.insertBefore(el.cloneNode(true),document.body.firstChild);});});})();